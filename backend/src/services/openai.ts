type GenerateMessagesParams = {
  platform: string;
  stage: string;
  girlInfo?: string;
};

export async function generateMessages({
  platform,
  stage,
  girlInfo,
}: GenerateMessagesParams): Promise<string[]> {
  // 🔒 MOCK-ГЕНЕРАЦИЯ (OpenAI временно отключён)

  if (stage === "first_message") {
    return [
      "Привет 🙂 У тебя очень приятная энергия",
      "Скажи честно, ты больше за спонтанные приключения или уютные вечера?",
      girlInfo
        ? `Я увидел, что тебе нравится ${girlInfo}. Это сразу цепляет`
        : "Ты выглядишь как человек, с которым легко начать разговор",
    ];
  }

  if (stage === "reply") {
    return [
      "Хм, это звучит интересно 👀",
      "Мне нравится, как ты это сформулировала",
      "Ты умеешь поддерживать разговор — это редкость",
    ];
  }

  if (stage === "date_invite") {
    return [
      "Кажется, пора перенести этот разговор за кофе ☕ Как смотришь?",
      "А давай продолжим это обсуждение вживую?",
      "Мне правда интересно с тобой — может, увидимся?",
    ];
  }

  // fallback
  return [
    "Хм, давай попробуем сформулировать это чуть иначе 🙂",
    "Интересный поворот",
  ];
}
